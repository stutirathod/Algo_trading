# --- Web server (serves the Flask API on Render) ---
Flask==3.1.2
flask-cors==6.0.2
gunicorn>=21.2.0

# --- Core data handling ---
pandas==2.3.2
numpy==2.2.6
pyarrow==23.0.1
python-dateutil==2.9.0.post0
pytz==2025.2
python-dotenv==1.2.1

# --- Feature engineering (confirmed used in engineer.py) ---
ta==0.11.0
scipy==1.16.1

# --- Models (confirmed used in pipeline.py / tree_models.py) ---
scikit-learn==1.7.1
xgboost==3.2.0

# --- Data collection (likely used in collector.py - verify) ---
yfinance==1.0
pandas-datareader==0.10.0

# --- EDA plotting (likely used in eda.py - verify) ---
matplotlib==3.10.6
seaborn==0.13.2

# --- OPTIONAL: only needed if you use --lstm or the hybrid ARIMA-LSTM model ---
# tensorflow-cpu>=2.13,<2.16
# statsmodels==0.14.6